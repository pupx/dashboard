import 'package:flutter/material.dart';

class FormBloqueados extends StatelessWidget {
  const FormBloqueados({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
